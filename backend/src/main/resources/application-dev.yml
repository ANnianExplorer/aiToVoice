spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aitovoice?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
    username: root
    password: ${DB_PASSWORD:yzh521}
  jpa:
    show-sql: true

jwt:
  secret: ${JWT_SECRET:myDefaultSecretKeyThatShouldBeChangedInProduction2026}

cors:
  allowed-origins: http://localhost:5173,http://localhost:8080

music:
  jamendo:
    client-id: ${JAMENDO_CLIENT_ID:}

logging:
  level:
    com.aitovoice: DEBUG
